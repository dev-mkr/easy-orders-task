import Kanban from "@/components/Kanban/Kanban";
import AddNewBlock from "@/components/Kanban/components/AddNewBlock";

function App() {
  return (
    <main>
      <AddNewBlock />
      <Kanban />
    </main>
  );
}

export default App;
